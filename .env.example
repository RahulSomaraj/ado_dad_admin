# Copy this file to `.env` and set the value for your environment.
# `.env` is gitignored and must exist for the app to start (loaded in main.dart).
#
# Base URL of the Ado-dad backend API (no trailing slash).
#   Local dev : http://localhost:3000
#   UAT       : https://uatapi.ado-dad.com   (admin: https://uatadmin.ado-dad.com)
API_BASE_URL=https://uatapi.ado-dad.com
